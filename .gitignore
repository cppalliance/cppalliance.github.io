_drafts/
_site/
.DS_Store
*.swp
Gemfile.lock
.sass-cache/
.ruby-version

# Ruby Bundler configuration
.bundle/

# JetBrains IDE project files
.idea/

# Ruby gems installed locally via Bundler
vendor/

# Local working directory for temporary project-related data.
# Users can keep notes, drafts, symlinks to upstream projects,
# and other materials here. Everything in this directory is
# personal and will never be committed to the repository.
local/

# Claude Code and agent instructions (per-user, not shared)
AGENTS.md
CLAUDE.md

# Playwright MCP artifacts
.playwright-mcp/